<div class="modal fade" id="postDeleteModal" tabindex="-1" role="dialog" aria-labelledby="postDeleteModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title text-danger" id="postDeleteModalLabel"><i class="fa fa-trash-o"></i> Delete Post</h4>
      </div>
      <div class="modal-body">
        <strong>Are you sure you want to delete this !</strong>
        <p>Warning! This will also deletes all associated comments </p>
        <p>Deleted post cannot be recovered</p>
        <div class="panel panel-info">
          <div class="panel-body">
            <p class="text-info">Post</p>
            <p><%= simple_format(post.post_content) %></p>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <%= link_to 'Delete', post, method: :delete, class: "btn btn-danger", remote: true %>
        <button type="button" class="btn btn-primary" data-dismiss="modal">Cancel</button>
      </div>
    </div>
  </div>
</div>