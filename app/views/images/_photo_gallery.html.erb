<div class="col-xs-6 col-sm-4 col-md-2">
  <%= link_to image_tag(image.photo.url(:square), class: 'img-thumbnail'), {action: 'display_image', controller: 'images', id: image.id, user: user.id},
      class: 'thumbnail', remote: true %>
</div>
