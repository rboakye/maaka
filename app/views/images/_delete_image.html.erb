<div class="modal fade" id="imageDeleteModal" tabindex="-1" role="dialog" aria-labelledby="imageDeleteModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h4 class="modal-title text-danger" id="postDeleteModalLabel"><i class="fa fa-trash-o"></i> Delete Image</h4>
      </div>
      <div class="modal-body">
        <strong>Are you sure you want to delete this image</strong>
        <p>Warning! This will also deletes all associated comments </p>
        <p>Deleted image cannot be recovered</p>
        <div class="panel panel-info">
          <div class="panel-body">
            <%= image_tag(image.photo.url(:medium), class: 'img-responsive') %>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <%= link_to 'Delete', image, method: :delete, class: "btn btn-danger", remote: true %>
        <button type="button" class="btn btn-primary" data-dismiss="modal">Cancel</button>
      </div>
    </div>
  </div>
</div>